import Button from "../baseComponent/Button";
const Invoice = (props) => {
  return (
    <>
      <p>
        If Use Method Component , Router Automatically Send object with Property
        Location , History and Match as Props To Component
      </p>
      <Button />
    </>
  );
};
export default Invoice;
